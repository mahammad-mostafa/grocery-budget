<li class="group">
  <a href="<%= group_path(group) %>">
    <img src="<%= group.icon %>" alt="<%= group.name %>"/>
    <div>
      <h2><%= group.name %></h2>
    </div>
    <span>$<%= group.total %></span>
  </a>
</li>